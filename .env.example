# MongoDB connection string (required)
# For local development: mongodb://localhost:27017/pillar
# For Docker Compose: mongodb://mongo:27017/pillar
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/pillar
MONGODB_URI=mongodb://localhost:27017/pillar

# Secret for signing JWT sessions (required)
# Generate with: openssl rand -base64 32
# Must be at least 32 characters long
AUTH_SECRET=generate-a-random-string-at-least-32-characters

# Public URL of the application (required)
# For local development: http://localhost:3000
# For production: https://pillar.example.com
AUTH_URL=http://localhost:3000

# Note: trustHost is enabled by default in this application
# This cannot be changed via environment variable
# See docs/environment-variables.md for details
# AUTH_TRUST_HOST=true

# AI-powered task/subtask generation (optional)
# If not set, AI features will be disabled for all users
# AI_API_KEY=your-api-key

# AI provider to use (optional)
# Options: openai (default), google
# Default: openai
# AI_PROVIDER=openai

# AI model to use (optional)
# OpenAI default: gpt-4.1-mini
# Google default: gemini-2.0-flash
# Override the default model for your chosen provider
# AI_MODEL=gpt-4.1-mini

# Comma-separated list of email addresses allowed to use AI features (optional)
# If not set, AI features are available to all users (when AI_API_KEY is configured)
# If set, only listed email addresses can use AI features
# AI_ALLOWED_EMAILS=user1@example.com,user2@example.com

# Web Push Notifications (optional)
# Required for background push notifications on mobile PWA
# Generate keys with: npx web-push generate-vapid-keys
# VAPID_PUBLIC_KEY=your-public-key
# VAPID_PRIVATE_KEY=your-private-key
# VAPID_SUBJECT=mailto:admin@pillar.example.com
